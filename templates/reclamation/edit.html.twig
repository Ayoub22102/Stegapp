{% extends 'base.html.twig' %}

{% block title %}Modifier Réclamation{% endblock %}

{% block body %}
    <div class="container my-4">
        <h1 class="mb-4">Modifier la Réclamation</h1>

        <div class="mb-4">
            {{ include('reclamation/_form.html.twig', {'button_label': 'Mettre à jour'}) }}
        </div>

        <div class="mt-4">
            <a href="{{ path('app_reclamation_index') }}" class="btn btn-secondary">Retour à la liste</a>
            {{ include('reclamation/_delete_form.html.twig', {'class': 'btn btn-danger'}) }}
        </div>
    </div>
{% endblock %}
